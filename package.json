{
  "name": "kimi-cli-visual",
  "version": "1.0.0",
  "description": "kimi-code改色插件",
  "scripts": {
    "test": "node test_shimmer.js",
    "patch": "python extract_blob.py && python patch_palette.py && python shimmerize.py && node test_shimmer.js && python rebuild_blob.py build/main.final.cjs build/blob.final.bin && python inject_exe.py build/blob.final.bin build/kimi.final.exe"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "postject": "^1.0.0-alpha.6"
  }
}
